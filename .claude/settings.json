{
  "permissions": {
    "allow": [
      "Read(./**)",
      "Write(./lesson-modules/**)",
      "Edit(./lesson-modules/**)",
      "Bash(ls:*)",
      "Bash(cat:*)",
      "Bash(head:*)",
      "Bash(wc:*)",
      "Bash(cursor *)",
      "Bash(open *)",
      "Bash(vercel *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && jq -r '.tool_input.file_path // empty' < /dev/stdin | xargs -I {} sh -c 'test -f \"{}\" && [ -f .prettierrc.json ] && ./node_modules/.bin/prettier --write --ignore-unknown --log-level=warn \"{}\" 2>/dev/null || true'"
          }
        ]
      }
    ]
  }
}
