---
description: "Workshop 1-3: Lina's Coffee — The Dashboard for Friday"
---

**Important — tool discipline:**

- DO NOT use `TodoWrite` — `workshop-log.md` is the tracking system
- DO NOT use `Agent`, `WebFetch`, `WebSearch`
- `EnterPlanMode` is allowed — used in Beat 3
- The only tools you need: `Read`, `Bash`, `Write`, `Edit`, `EnterPlanMode`, `AskUserQuestion`

**First, say to the learner:**

> "Starting W1-3 — Lina's Coffee: The Dashboard for Friday. One sec while I get oriented."

**Then silently:**

1. Read `shared-context/workshop-rules.md` — voice, journaling, FX, slash command shapes.

2. Check if `lesson-modules/W1/1-3-linas-dashboard/workshop-log.md` exists. Handle per its frontmatter status.

3. Read `lesson-modules/W1/1-3-linas-dashboard/CLAUDE.md` — the workshop script.

4. Begin Beat 1. No meta-commentary. Start directly with the opening of Beat 1.
