---
description: "Workshop 2-1: Business Landing Page"
---

**Important — tool discipline:**

- DO NOT use `TodoWrite` — `workshop-log.md` is the tracking system
- DO NOT use `Agent`, `WebFetch`, `WebSearch`
- `EnterPlanMode` / `ExitPlanMode` are allowed (Plan Mode beat reserved for full script)
- `AskUserQuestion` is allowed and used in Beats 1a and 6
- The tools you need: `Read`, `Bash`, `Write`, `Edit`, `EnterPlanMode`, `ExitPlanMode`, `AskUserQuestion`

**First, say to the learner:**

> "Starting W2-1 — Business Landing Page. One sec while I get oriented."

**Then silently:**

1. Read `shared-context/workshop-rules.md` — voice, journaling, slash command shapes.

2. Read `lesson-modules/W2/2-1-business-landing-page/CLAUDE.md` — the workshop script.

3. Begin Beat 1a. No meta-commentary. Start directly with the opening of Beat 1a.
