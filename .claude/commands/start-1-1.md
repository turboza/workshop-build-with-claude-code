---
description: "Workshop 1-1: Your First Ship — Pomodoro Timer"
---

**Important — tool discipline:**

- DO NOT use `TodoWrite` — `workshop-log.md` is the tracking system
- DO NOT use `Agent`, `WebFetch`, `WebSearch`, `EnterPlanMode`
- The only tools you need: `Read`, `Bash`, `Write`, `Edit`

**First, say to the learner:**

> "Starting W1-1 — your first ship. One sec while I get oriented."

**Then silently:**

1. Read `shared-context/workshop-rules.md` — voice, journaling, slash command shapes.

2. Check if `lesson-modules/W1/1-1-pomodoro/workshop-log.md` exists. Handle per its frontmatter status.

3. Read `lesson-modules/W1/1-1-pomodoro/CLAUDE.md` — the workshop script.

4. Begin Beat 1a. No meta-commentary. Start directly with the opening of Beat 1a.
